name: 'Build Maven'
description: 'Build gradle'

runs:
  using: 'composite'
  steps:
    - run: |
        echo "clean src/main/resources/static"
        rm -rf src/main/resources/static
        echo "clean vue/dist"
        rm -rf vue/dist
      shell: bash
