<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.github.bestheroz.sample.web.admin.menu.AdminMenuDAO">
    <select id="getList" resultType="GetSampleMenuMstVOListResponseVO" parameterType="TableSampleMenuMstVO">
        SELECT SMM.MENU_ID
        , SMM.MENU_NM
        , SMM.MENU_TYP
        , SMM.PAR_MENU_ID
        , SMM.USE_YN
        , SMM.POWER
        , SMM.DISP_SEQ
        , SMM.URL
        , SMM.REMARK1
        , SMM.UPD_DT
        , SMM.UPD_MEMBER_ID
        FROM SAMPLE_MENU_MST SMM
        <where>
            <if test="menuId != null">
                AND SMM.MENU_ID = #{menuId, jdbcType=INTEGER}
            </if>
            <if test="@org.apache.commons.lang3.StringUtils@isNotEmpty( menuNm )">
                AND SMM.MENU_NM = #{menuNm, jdbcType=VARCHAR}
            </if>
        </where>
        ORDER BY SMM.UPD_DT DESC
    </select>
</mapper>